const ar=document.documentElement.lang==='ar';const menu=document.querySelector('.menu'),nav=document.querySelector('.links');function closeMenu(){menu?.setAttribute('aria-expanded','false');nav?.classList.remove('open')}menu?.addEventListener('click',()=>{const open=menu.getAttribute('aria-expanded')!=='true';menu.setAttribute('aria-expanded',String(open));nav.classList.toggle('open',open)});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()});
if(!matchMedia('(prefers-reduced-motion:reduce)').matches&&'IntersectionObserver'in window){document.documentElement.classList.add('motion');const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target)}}),{threshold:.05});document.querySelectorAll('.reveal').forEach(x=>io.observe(x))}
let opener;const dialog=document.querySelector('.lightbox');document.querySelectorAll('[data-lightbox]').forEach(b=>b.addEventListener('click',()=>{opener=b;dialog.querySelector('img').src=b.querySelector('img').src;dialog.showModal()}));dialog?.querySelector('.close').addEventListener('click',()=>dialog.close());dialog?.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});dialog?.addEventListener('close',()=>opener?.focus());document.querySelectorAll('[data-service]').forEach(a=>a.addEventListener('click',()=>{document.querySelector('[name=service]').value=a.dataset.service}));
const form=document.querySelector('form');form?.addEventListener('submit',e=>{e.preventDefault();if(!form.reportValidity())return;const data=new FormData(form);const name=String(data.get('name')).trim();if(!name){form.querySelector('[name=name]').setCustomValidity(ar?'اكتب اسمك':'Please enter your name');form.reportValidity();return}const msg=ar?`مرحبًا ${form.dataset.business}\nالاسم: ${name}\nالخدمة: ${data.get('service')}\nاليوم المفضل: ${data.get('date')||'لم يحدد'}\nالتفاصيل: ${String(data.get('details')).trim()}\nأرجو تأكيد التفاصيل والتكلفة والتوافر.`:`Hello ${form.dataset.business}\nName: ${name}\nService: ${data.get('service')}\nPreferred date: ${data.get('date')||'Not specified'}\nDetails: ${String(data.get('details')).trim()}\nPlease confirm the details, price and availability.`;const result=form.querySelector('.result');result.hidden=false;result.querySelector('.message').textContent=msg;result.querySelector('a').href=`https://wa.me/${form.dataset.phone}?text=${encodeURIComponent(msg)}`;form.querySelector('.feedback').textContent=ar?'الرسالة جاهزة للمراجعة. لم تُرسل بعد.':'Your message is ready for review. Nothing has been sent.';result.scrollIntoView({behavior:'smooth',block:'nearest'})});form?.querySelector('[name=name]').addEventListener('input',e=>e.target.setCustomValidity(''));const date=form?.querySelector('[name=date]');if(date){const d=new Date();date.min=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
